# Core
fastmcp>=0.2.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# LLM
openai>=1.30.0

# Data / storage
# sqlite3 is stdlib; psycopg[binary] kept commented for PG-readiness
# psycopg[binary]>=3.1.0

# HTTP / validation
httpx>=0.27.0
tldextract>=5.1.0

# Utilities
python-dateutil>=2.9.0

# Dev / test
pytest>=8.0.0
pytest-asyncio>=0.23.0
ruff>=0.4.0

# v1 (added in next phase) — kept here for forward visibility
# chromadb>=0.5.0
# sentence-transformers>=2.7.0
# reportlab>=4.1.0
